1 2 e 3) As três primeiras tratam de ABSTRAÇÃO. 

4) Os computadores foram inventados para ampliar as capacidades humanas, não apenas para repetir o que já sabemos fazer. Embora realmente dependam de programação para executar tarefas específicas, sua invenção trouxe uma série de avanços significativos. 
  Assim, os computadores foram inventados não apenas para repetir tarefas humanas, mas para expandir nossas habilidades, aumentar nossa eficiência e abrir novas possibilidades que antes eram inimagináveis. Eles atuam como ferramentas poderosas que, quando combinadas com a criatividade e o raciocínio humano, podem gerar inovações e avanços em diversas áreas.

5) (A questão aqui não é a subjetividade) Ora, apenas um médico pode tomar decisões médicas; no caso da análise de impréstimo - cuja negativa, no Brasil, precisa ser justificada - um algoritmo ou modelo de IA complica a explicação. 
   Se existe algum modelo usável em algum dos casos em pauta? Para o caso médico, não; para o caso da análise do empréstimo: Árvore de decisões - algoritmo perfeito para explicar decisões, SVM, KNN, Perception, Adeline. 

6) A principal diferença entre um arquivo "binário" e um arquivo "de texto" está na forma como os dados são armazenados e interpretados. Um arquivo de texto contém informações representadas em caracteres legíveis por humanos, como letras, números e símbolos. Ele utiliza codificações, como ASCII ou UTF-8, que mapeiam cada caractere a um número específico. Isso torna o arquivo acessível e facilmente editável em qualquer editor de texto, permitindo que as pessoas leiam e compreendam seu conteúdo sem a necessidade de software especializado.
   Por outro lado, um arquivo binário armazena dados em um formato que não é facilmente legível. Esse tipo de arquivo pode conter uma combinação de informações, incluindo texto, imagens, áudio ou vídeo, organizadas de maneira que um computador consiga entender, mas que não faz sentido quando lidas diretamente por humanos. Os arquivos binários são projetados para serem processados por programas específicos, como reprodutores de mídia ou editores de imagens, e geralmente possuem extensões como .exe, .jpg ou .mp3.
   Em resumo, a distinção fundamental reside na legibilidade: enquanto os arquivos de texto são simples e acessíveis, os arquivos binários são complexos e requerem software especializado para interpretação.
   Apesar de ambos serem formados por bits (zeros e uns), em um aqrquivo "de texto" os bits serão sempre representações de caracteres.

7) A diferença entre formatos proprietários e abertos está na acessibilidade e liberdade de uso. Formatos proprietários, como os do Microsoft Word, têm restrições e exigem software específico, muitas vezes pago. Em contraste, formatos abertos, como CSV e PDF/A, têm especificações publicamente disponíveis, permitindo que qualquer um os utilize e modifique sem barreiras.
   No contexto de dados públicos, formatos abertos são preferíveis porque garantem que cidadãos e organizações possam acessar e usar as informações livremente, promovendo transparência e responsabilidade. Por outro lado, dados em formatos proprietários podem criar obstáculos, dificultando o acesso e a análise, o que pode prejudicar a confiança nas instituições. Assim, optar por formatos abertos para dados públicos assegura que esses dados sejam acessíveis, reutilizáveis e mantidos a longo prazo.
   Especificação pública (formato aberto) x Controle e necessidade de licenciamento (formato proprietário).

8) No contexto da computação, a diferença entre um protocolo de comunicação aberto e um proprietário está na acessibilidade e na liberdade de uso. Protocolos abertos, como HTTP e TCP/IP, têm especificações publicamente disponíveis, permitindo que qualquer desenvolvedor os utilize e modifique livremente. Em contrapartida, protocolos proprietários são controlados por empresas específicas, que impõem restrições de uso e exigem licenças para implementação, limitando a capacidade de inovação.
   No cenário da liberdade econômica e do desenvolvimento de novas aplicações para a Internet, os protocolos abertos são fundamentais. Eles promovem um ambiente de inovação e concorrência, permitindo que startups e desenvolvedores independentes criem soluções sem barreiras significativas. Já os protocolos proprietários podem restringir essa liberdade, inibindo a criatividade e aumentando custos para consumidores e desenvolvedores. Assim, a adoção de protocolos abertos é crucial para fomentar um ecossistema acessível e dinâmico, beneficiando todos os envolvidos.

9) a) Um sistema operacional (SO) é um software fundamental que gerencia os recursos de um computador e serve como intermediário entre o usuário, os programas e o hardware. Ele controla componentes físicos, como a CPU, a memória, o armazenamento e dispositivos de entrada/saída, garantindo que eles funcionem de maneira eficiente e coordenada.
      A relação entre o sistema operacional, os programas e os componentes físicos é essencial. O SO fornece uma interface que permite aos usuários e aplicativos interagir com o hardware sem precisar entender os detalhes técnicos de como cada componente funciona. Ele gerencia tarefas como alocação de memória, execução de processos e controle de dispositivos, permitindo que múltiplos programas sejam executados simultaneamente de forma estável.
      Assim, o sistema operacional é crucial para a operação de um computador, pois possibilita que programas sejam executados de maneira integrada, facilitando o uso e maximizando o desempenho do hardware.
   b) Não! Vários...
   c) Sim, vender computadores obrigatoriamente ativados com um sistema operacional pode ser considerado "venda casada" em certas circunstâncias. A venda casada ocorre quando um fornecedor condiciona a venda de um produto à aquisição de outro, limitando a liberdade de escolha do consumidor. No contexto de computadores, isso se manifesta quando um fabricante ou revendedor exige que o cliente compre um computador com um sistema operacional específico já instalado e ativado, sem a opção de adquirir apenas o hardware ou escolher um sistema operacional diferente.
      Essa prática pode ser problemática por várias razões. Primeiramente, ela pode limitar a concorrência, já que impede que o consumidor opte por alternativas mais econômicas ou adequadas às suas necessidades. Além disso, pode desvalorizar as opções de sistemas operacionais de código aberto, que são gratuitos e oferecem maior flexibilidade.
      Em muitos países, a legislação de defesa do consumidor proíbe a venda casada, reconhecendo que os consumidores têm o direito de escolher os produtos que desejam. Assim, para evitar implicações legais e garantir a satisfação do cliente, é geralmente preferível que os fabricantes e revendedores ofereçam opções, permitindo que os consumidores escolham como desejam adquirir e configurar seus dispositivos.
      *STJ: NÃO é venda casada - item essencial para o funcionamento / *Professor: É venda casada SIM!... Lobby... 

10) a) Sim, quando tratam dados COLETADOS no BR. 
    b) * servidores de aplicação: donas de aplicativos
      1) Opinião, forma aí. 
      2) Opinião, forma aí. 

11) a) As estruturas de repetição em Python, como for e while, são ferramentas poderosas que facilitam a execução de tarefas repetitivas, promovendo a eficiência e a clareza no código. A escolha entre elas depende das necessidades específicas da aplicação, como a natureza da sequência a ser percorrida e as condições que governam a repetição.
    b) Em Python, os blocos de comando são definidos pelo uso de ":" e INDENTAÇÃO (aquele espacinho), que é uma característica essencial da linguagem. Diferente de outras linguagens que usam chaves {} ou palavras-chave específicas para delimitar blocos de código, Python utiliza espaços ou tabulações para indicar a estrutura hierárquica do código.
      A indentação deve ser consistente ao longo do código; ou seja, todos os comandos do mesmo bloco devem ter o mesmo nível de indentação, seja usando espaços ou tabulações, mas não uma combinação de ambos.
    c) O operador + é amplamente utilizado em programação e matemática, realizando duas principais operações: adição aritmética e concatenação de strings.
       1. Adição Aritmética: Esta operação envolve a soma de valores numéricos. Quando o operador + é aplicado a números, ele calcula a soma total. Por exemplo, em uma expressão como 5 + 3, o resultado é 8. Essa operação é fundamental em cálculos matemáticos e é suportada por diversos tipos de dados, como inteiros e números de ponto flutuante. A adição aritmética é uma das operações básicas em qualquer linguagem de programação e é frequentemente utilizada em algoritmos, fórmulas e análises de dados.
       2. Concatenação de Strings: Em muitas linguagens de programação, o operador + também é utilizado para unir ou concatenar cadeias de caracteres, ou strings. Por exemplo, ao realizar “Olá, ” + “mundo!”, o resultado é a string combinada "Olá, mundo!". Essa operação permite que os programadores criem textos dinâmicos e mensagens a partir de diferentes variáveis e entradas do usuário. A concatenação de strings é essencial para a manipulação de texto, construção de interfaces de usuário e geração de relatórios, entre outras aplicações.
    d) Uma função em Python é um bloco de código (um pedaço de programa) que realiza uma tarefa específica e pode ser reutilizado em diferentes partes de um programa. As funções permitem que você agrupe operações relacionadas, tornando o código mais modular e fácil de entender.
    e) Para definir uma função em Python, você utiliza a palavra-chave def, seguida pelo nome da função e parênteses que podem incluir parâmetros. Em seguida, você adiciona dois pontos (:) e, dentro de um bloco indentado, coloca o código que a função deve executar.
    f) Em Python, as principais estruturas de tomada de decisão são: if; if-else e if-elif-else. (ISSO CAI NA PROVA, não assim, mas cai)
       (Se ligar nos exemplos  pra fazer)
      
